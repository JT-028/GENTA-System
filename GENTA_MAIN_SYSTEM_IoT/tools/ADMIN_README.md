# GENTA Admin — Archived Admin Docs & Quick Notes

This repository previously included several admin documentation files. They have been archived under:

`backups/archived_admin_docs/`

Files archived:

- `QUICK_START_LOGIN.md` — quick-start login guide
- `ADMIN_LOGIN_README.md` — detailed login/security documentation
- `ADMIN_IMPLEMENTATION_SUMMARY.md` — implementation summary / changelog

Purpose:
- Keep the main repository clean while preserving the documentation for audits or later reference.

What to do next:
- If you'd like these restored to the repository root, move them back from `backups/archived_admin_docs/`.
- If you want a shorter consolidated README added to the repository root, edit this file.

Security reminder:
- Change default admin credentials immediately after first login.
- Use environment variables to set `ADMIN_USERNAME` and `ADMIN_PASSWORD_HASH`.

Location of admin docs:
`backups/archived_admin_docs/`

